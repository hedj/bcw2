# The configuration of pytest, which make test runs on tools/tests.
[pytest]
testpaths = tools/tests
# The tests import the tools, such as bcw and linemap, and the helpers in tools/tests.
pythonpath = tools tools/tests
# The test classes end in Test, such as TargetTest.
python_classes = *Test
